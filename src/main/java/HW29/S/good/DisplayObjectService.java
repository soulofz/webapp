package HW29.S.good;

public class DisplayObjectService {
    public void displayInfo(Home home){
        System.out.println("ID:" + home.getId() + "Street:" + home.getStreet() + "Flours:" + home.getFlours());
    }
}
